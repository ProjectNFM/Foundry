# Legacy Experiments

This directory contains the original sequentially-numbered experiment files
(001–023) from before the inbox-based workflow was adopted.

These files are **frozen for reference**. They use the old sequential naming
convention and internal cross-links that assume a flat `experiments/` layout.

For new experiments, use the `create-experiment` skill which places files in
`experiments/inbox/`.
