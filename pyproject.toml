[project]
name = "foundry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "brainsets",
 "ipykernel>=7.1.0",
 "lightning>=2.6.0",
 "torch>=2.9.1",
 "torch-brain",
]

[tool.uv.sources]
torch-brain = { git = "ssh://git@github.com/neuro-galaxy/torch_brain" }
brainsets = { git = "ssh://git@github.com/neuro-galaxy/brainsets" }
