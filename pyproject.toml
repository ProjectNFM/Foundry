[project]
name = "foundry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "brainsets",
    "ipykernel>=7.1.0",
    "torch>=2.9.1",
    "pytorch-brain",
]

[tool.uv.sources]
pytorch-brain = { git = "ssh://git@github.com/neuro-galaxy/torch_brain", branch = "vinam/dataset_overhaul" }
brainsets = { git = "ssh://git@github.com/neuro-galaxy/brainsets", branch = "vinam/datasets" }
