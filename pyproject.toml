[project]
name = "foundry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "eegdash>=0.5.0",
    "ipykernel>=7.1.0",
    "torch>=2.9.1",
]
