# Repository Guidelines

- Run all Python commands through `uv` (for example, use `uv run python ...` and `uv run pytest ...`).
- Before finishing any task, run `uv run ruff format` and `uv run ruff check`.
