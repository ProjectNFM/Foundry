_target_: foundry.training.FoundryModule

learning_rate: ${hyperparameters.learning_rate}
weight_decay: ${hyperparameters.weight_decay}
cwt_lr_multiplier: ${hyperparameters.cwt_lr_multiplier}
backbone_learning_rate: ${hyperparameters.backbone_learning_rate}
backbone_components: ${oc.select:hyperparameters.backbone_components,null}
scheduler_name: ${hyperparameters.scheduler_name}
warmup: ${hyperparameters.warmup}
warmup_fraction: ${oc.select:hyperparameters.warmup_fraction,null}
start_lr_factor: ${hyperparameters.start_lr_factor}
hold: ${hyperparameters.hold}
hold_scheduler_type: ${hyperparameters.hold_scheduler_type}
decay: ${hyperparameters.decay}
end_lr_factor: ${hyperparameters.end_lr_factor}
scheduler_interval: ${hyperparameters.scheduler_interval}
adapter_warmup_steps: ${oc.select:hyperparameters.adapter_warmup_steps,0}
