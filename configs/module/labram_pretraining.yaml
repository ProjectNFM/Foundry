_target_: foundry.training.LaBraMPretrainingModule

learning_rate: ${hyperparameters.learning_rate}
weight_decay: ${hyperparameters.weight_decay}
masking:
  _target_: foundry.tasks.masking.RandomTokenMasking
  mask_ratio: 0.5
symmetric_masking: true
vqnsp_ckpt_path: ???
