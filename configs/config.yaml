defaults:
  - model: null
  - data: null
  - module: classification
  - cluster: default
  - logger: wandb
  - _self_
  - trainer: default
  - profiling: default
  - experiment: ???

hyperparameters:
  batch_size: ???
  sequence_length: ???
  patch_duration: ???
  sampling_rate: ???
  fold_number: ???
  num_workers: ???
  learning_rate: ???  
  weight_decay: ???

stage:
  skip: false

run:
  # Keep fresh runs independent unless resume is explicitly requested.
  # SLURM restarts still auto-resume from last.ckpt in main.py.
  resume_if_checkpoint_exists: false
  # Reuse a WandB run ID derived from run.name.
  # Keeps dashboards continuous for preempted/relaunched runs with same name.
  resume_wandb_if_name_matches: false
  # A100 benefits from Tensor Core-enabled FP32 matmul kernels.
  float32_matmul_precision: high
  # Optional: checkpoint path to use for test evaluation.
  # If not provided, uses best checkpoint from training. Can be overridden on CLI.
  # Example: python main.py run.eval_checkpoint=/path/to/checkpoint.ckpt
  eval_checkpoint: null
