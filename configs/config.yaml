defaults:
  - model: null
  - data: null
  - logger: wandb
  - _self_
  - trainer: default
  - profiling: default
  - experiment: ???

hyperparameters:
  batch_size: ???
  window_length: ???
  patch_duration: ???
  fold_number: ???
  num_workers: ???

task:
  learning_rate: 1e-4
  weight_decay: 0.01

stage:
  skip: false

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: outputs/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
