_target_: foundry.data.datamodules.AjileDataModule

root: ./data/processed/
batch_size: ${hyperparameters.batch_size}
num_workers: 5
pin_memory: false 
window_length: ${hyperparameters.window_length}
seed: 42

split_type: intersubject
task_type: behavior
fold_num: ${hyperparameters.fold_number}

