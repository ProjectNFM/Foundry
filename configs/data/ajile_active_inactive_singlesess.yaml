_target_: foundry.data.datamodules.AjileDataModule

root: ./data/processed/
batch_size: ${hyperparameters.batch_size}
num_workers: 0
pin_memory: false 
window_length: ${hyperparameters.window_length}
seed: 42

split_type: intrasession
task_type: active_vs_inactive
fold_number: ${hyperparameters.fold_number}
recording_ids: 
 - "AJILE12_P01_20000102_ses3_pose_trajectories"

