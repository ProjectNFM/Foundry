_target_: foundry.models.GRU

num_channels: ${hyperparameters.num_channels}
num_samples: 128
input_proj_dim: 128
hidden_size: 128
num_layers: 2
bidirectional: true
dropout_rate: 0.3
channel_mode: fixed
