# @package _global_
#
# Experiment 022: KempSleep baselines (scratch) and finetuning from exp 021
# pretrained CWT-CNN models with dynamic/disabled channel embeddings.
# Session embeddings disabled for all conditions.
#
# 4 conditions: 2 channel_emb_mode × {scratch, finetuned}
#
# Scratch baselines (2 jobs: ch-disabled, ch-dynamic):
#   uv run python main.py experiment=sleep_staging/poyo_kemp_finetune_cwt_dynch \
#       run.init_mode=scratch run.pretrained_checkpoint=null -m
#
# Finetuning from exp 021 pretrained checkpoints (2 jobs):
#   uv run python main.py experiment=sleep_staging/poyo_kemp_finetune_cwt_dynch \
#       run.init_mode=finetuned -m

defaults:
  - override /model: poyo_eeg
  - override /model/tokenizer: per_channel_cwt_cnn
  - override /model/session_emb: disabled
  - override /data: kemp_sleep_edf/allsess
  - override /hydra/launcher: slurm_default

task_configs:
  - sleep_stage_5class

pretrained_checkpoints:
  disabled: /network/scratch/s/sobralm/runs/PRETRAIN_CWT_DYNAMIC_CHANNEL_EMB/pretrain_cwt_dynch_ch-disabled/checkpoints/last.ckpt
  dynamic: /network/scratch/s/sobralm/runs/PRETRAIN_CWT_DYNAMIC_CHANNEL_EMB/pretrain_cwt_dynch_ch-dynamic/checkpoints/last.ckpt

model:
  zero_output_timestamps: true
  normalize_inputs: true
  channel_emb_mode: disabled

data:
  root: ${oc.env:SLURM_TMPDIR}/brainsets/processed
  split_type: intersubject
  task_type: sleep_stage

stage:
  skip: false

run:
  name: kemp_022_${run.init_mode}_ch_${model.channel_emb_mode}
  group: KEMP_FINETUNE_CWT_DYNCH
  tags: [sleep_staging, poyo, kemp, cwt_cnn, dynamic_channel_emb, exp022]
  log_level: INFO
  project: foundry_finetuning
  compile: default
  pretrained_checkpoint: ${pretrained_checkpoints.${model.channel_emb_mode}}
  pretrained_transfer_mode: permissive
  freeze_pretrained: false
  init_mode: finetuned

class_weights:
  mode: auto
  smoothing: 1.0

hyperparameters:
  batch_size: 512
  sequence_length: 2.0
  patch_duration: null
  sampling_rate: null
  fold_number: 0
  num_workers: 6
  num_channels: null
  session_configs: null
  learning_rate: 1e-4
  weight_decay: 0.01

trainer:
  max_epochs: 1000
  precision: bf16-mixed
  callbacks:
    early_stopping:
      monitor: val/sleep_stage_5class_f1
      mode: max
      patience: 20
    model_checkpoint:
      monitor: val/sleep_stage_5class_f1
      mode: max
      auto_insert_metric_name: false
      filename: "best-epoch{epoch:03d}-val_f1_{val/sleep_stage_5class_f1:.4f}"

hydra:
  sweeper:
    params:
      model.channel_emb_mode: disabled, dynamic
  launcher:
    tasks_per_node: 1
    cpus_per_task: 6
    mem_gb: 32
    gres: gpu:l40s:1
    timeout_min: 720
    max_num_timeout: 5
    setup:
      - "set -a"
      - "source .env"
      - "set +a"
      - "export RANK=0"
      - "uv run python -m foundry.tools.stage_data --experiment sleep_staging/poyo_kemp_finetune_cwt_dynch"
